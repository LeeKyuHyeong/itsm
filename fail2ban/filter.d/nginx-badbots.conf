[Definition]
# 400 Bad Request (TLS probe, SSH probe 등 비정상 요청) 감지
# 요청 줄이 바이너리(\x16\x03...)여도 nginx 가 \xNN 으로 이스케이프하므로 "[^"]*" 로 잡힌다.
# 구버전 2번째 정규식(\\x 포함 줄 전부)은 상태코드와 무관해 정상 200 줄까지 잡을 수 있어 제거 (2026-09-16 전수조사 P7).
failregex = ^<HOST> - \S+ \[[^\]]*\] "[^"]*" 400 \d+
ignoreregex =

[Definition]
# 404 반복 요청 감지 (nginx 기본 combined 로그 형식, 날짜는 fail2ban 이 잘라냄 → \[[^\]]*\])
# 구버전 정규식 `.*" (GET|POST|HEAD) .* 404` 는 따옴표 뒤 공백을 요구해 한 번도 매칭되지 않았다 (2026-09-16 전수조사 P7).
failregex = ^<HOST> - \S+ \[[^\]]*\] "(?:GET|POST|HEAD) [^"]*" 404 \d+
ignoreregex =

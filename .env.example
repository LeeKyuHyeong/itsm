# ── Database ──
DB_HOST=db:3306
DB_USERNAME=root
DB_PASSWORD=<your-db-password>

# ── JWT ──
JWT_SECRET=<base64-encoded-256bit-secret>

# ── CORS ──
CORS_ORIGINS=https://itsm.example.com

# ── Docker Hub ──
DOCKERHUB_USERNAME=<your-dockerhub-username>
